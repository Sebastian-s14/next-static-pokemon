import type { NextPage } from 'next'

import { Layout } from '../../components/layouts'

const FavoritosPage: NextPage = () => {
    return (
        <Layout title="Favoritos">
            <div>Página de favortios</div>
        </Layout>
    )
}

export default FavoritosPage
